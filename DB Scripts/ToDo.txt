PRIORITY:
	GET/CREATE PROCS:
		--GENERATE MULTIPLE CERTIFICATES AT ONCE
		--GENERATE ORDER & ITEMS
		--GET/CREATE CUSTOMER
		--GET/CREATE STAFF
		--POPULATE STAFF_LINK TABLE WHEN CREATING STAFF
		--DYNAMICALLY ASSIGN CERT TO CUSTOMER
			--MANUALLY SET ALL PARAMS
		--GET/CREATE CHANNEL
		--GET/CREATE PROMOTION
		GET/CREATE ROLE
		GET/CREATE STAFF_TYPE
		
	


LATER:
	GET TOTAL VALUE OF GENERATED CERTIFICATES AT ONE TIME IN ONE VENUE -- CAN BE TRACKED BACK (SUM) WHEN REDEEMED FOR ANALYTICS
	ANALYTICS -- MONTHLY REPORTS

